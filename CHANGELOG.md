# Changelog

## 0.2.0

- 将用量状态移到 VS Code 右侧状态栏。
- 状态栏改为显示 `$(pie-chart)` 图标和剩余比例。
- 悬浮详情改为左右对齐的 Markdown 表格。
- 新增 `apiTokenUsage.percentageDecimalPlaces` 配置。
- 新增 `apiTokenUsage.statusBarPriority` 配置，用于调整与 GitHub Copilot 等右侧状态栏项目的相对位置。
